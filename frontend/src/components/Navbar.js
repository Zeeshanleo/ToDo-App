import { Typography } from "@mui/material";

const Navbar = () => {
  return (
    <header>
      <Typography variant="h1" align="center">
        Todo App
      </Typography>
    </header>
  );
};

export default Navbar;
